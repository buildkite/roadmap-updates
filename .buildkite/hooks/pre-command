#!/bin/bash

# Diagnostics only — actual token setup is in the environment hook.
if [[ -n "${BUILDKITE_AGENT_ACCESS_TOKEN:-}" && -n "${BUILDKITE_AGENT_ENDPOINT:-}" ]]; then
  echo "~~~ :mag: Hosted Models: credentials set via environment hook"
fi
